-- TODO: I guess refactor this to something string-based to get similar types
module Blocks () where
-- tasks, blocks

-- -- tasks
-- tasks = [concat_ drop_]
--     where
--         -- tamandu
--         -- concat_ :: [a] -> [a] -> [a]
--         concat_ = (++)
--         -- drop_ :: Int -> [a] -> [a]
--         drop_ = drop

-- -- building blocks
-- blocks = [const_ flip_ true_ false_ not_]
--     where
--         const_ = const
--         flip_ = reverse
--         true_ = True
--         false_ = False
--         not_ = not
